import { describe, it } from 'vitest'

describe('Form card', () => {
  it('renders the content for props: cardTitle, footerMessage, footerLinktext and footerLinkSendTo', () => {})

  it('renders the child component passed as children', () => {})

  it('the prop linkSendTo properly redirects', () => {})
})
