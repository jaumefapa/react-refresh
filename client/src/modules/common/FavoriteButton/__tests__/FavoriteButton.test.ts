import { describe, it } from 'vitest'

describe('Favorite Button', () => {
  it('renders the red favorite button if isFavorite true', () => {})
  it('renders the white favorite button if isFavorite false', () => {})
  it('changes the fav state onClick', () => {})
})
