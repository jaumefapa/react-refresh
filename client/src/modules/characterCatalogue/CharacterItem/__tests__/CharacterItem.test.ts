import { describe, it } from 'vitest'

describe('CharacterItem', () => {
  it('renders the CharacterItem', () => {})

  it('The favorite button changes the favorite icon when clicked', () => {})

  it('If a character item is clicked, the user is redirected to the character detail page', () => {})
})
