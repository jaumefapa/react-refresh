import { describe, it } from 'vitest'

describe('AuthenticatedRoutes', () => {
  it('logouts if there"s no token in the state neither in the local storage', () => {})

  it('renders the AuthenticatedRoutes if there"s token in the redux state', () => {})

  it('renders the AuthenticatedRoutes if there"s no token in the redux state but there"s token in the session storage', () => {})

  it('renders <p>Loading...</> when the userInfo is loading', () => {})
})
