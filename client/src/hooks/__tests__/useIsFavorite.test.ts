import { describe, it } from 'vitest'

describe('useIsFavorite', () => {
  it('isFavorite return a boolean', () => {})

  it('setIsFavorite', () => {})
  it('setIsFavorite', () => {})
  it('setIsFavorite', () => {})
})
