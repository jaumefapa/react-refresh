import { describe, it } from 'vitest'

describe('Login Page', () => {
  it('Login page gets rendered', () => {})

  it('Login page information gets rendered', () => {})

  it('Submit button is not clickable if form is not properly filled', () => {})

  it('Form can be filled', () => {})

  it('Form can be submitted', () => {})

  it('If form is successful, user gets redirected to characters page', () => {})

  it('If form is successful, token gets stored in session storage', () => {})

  it('If form fails, information message is displayed', () => {})
})
