import { describe, it } from 'vitest'

describe('ErrorPage', () => {
  it('ErrorPage gets rendered', () => {})

  it('ErrorPage information gets rendered', () => {})

  it('User gets redirected to login page when clicking login link', () => {})

  it('User gets redirected to register page when clicking register link', () => {})

  it('If user goes to an unknown route, they get redirected to the error page', () => {})
})
