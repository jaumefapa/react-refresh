import { describe, it } from 'vitest'

describe('CharacterCatalogue', () => {
  it('Character details gets rendered', () => {
  })
  
  it('Characters information gets rendered', () => {});

  it('The favorite button changes favorite state when clicked', () => {});

  it('If a character item is clicked, the user is redirected to the character detail page', () => {});
});